# devengine

Base compartilhada para aplicações Go do ecossistema Tecno.

O repositório está sendo inicializado com runtime, módulos, PostgreSQL, HTTP, health/readiness, migrations separadas entre engine e aplicação, graceful shutdown e CLI de scaffold.
